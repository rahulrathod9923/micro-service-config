spring:
 application:
  name: Config-Default

# write repeatative properties of services here
eureka:
  instance:
    prefer-ip-address: true
  
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

